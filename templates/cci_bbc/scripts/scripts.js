var Popout = new Class({
	Implements: [Options, Events],
	options: {
		event: 'click'
	},
	
	container: null,
	elements: null,
	
	initialize: function(adsf, asdf, options) {
		this.setOptions(options);
	}

	

});